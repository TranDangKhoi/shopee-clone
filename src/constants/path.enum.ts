export enum path {
  home = "/",
  user = "/user",
  changePassword = "/user/change-password",
  orderHistory = "/user/order-history",
  profile = "/user/profile",
  login = "/login",
  register = "/register",
  logout = "/logout",
  productDetail = "/:slug",
  cart = "/cart",
}
